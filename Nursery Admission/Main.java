#include<iostream>
#include<string.h>
using namespace std;
int main()
{
char s[50];
cin>>s;
cout<<"The number of letters in the name is "<<strlen(s);// strlen() is a string funcyion which returns the number of the characters present in the string which accepts the input string s as an argument.
}
